{
  "name": "vite-ssr-playground",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build:client": "vite build --outDir dist/client --ssrManifest",
    "build:server": "vite build --outDir dist/server --ssr renderer/index.js",
    "build": "npm run build:client && npm run build:server",
    "preview": "node bundle.js && vite preview --outDir dist/client"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "chalk": "^4.1.2",
    "esbuild": "^0.17.19",
    "express": "^4.18.2",
    "sirv": "^2.0.3",
    "vite": "^4.3.6",
    "vue": "^3.3.2"
  }
}
