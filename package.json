{
  "name": "vite-ssr-playground",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build:server": "vite build --outDir dist/server --ssr server/index.js",
    "build": "npm run build:server && node bundle.js",
    "preview": "npm run build && vite preview --outDir dist/client"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "chalk": "^4.1.2",
    "esbuild": "^0.17.19",
    "express": "^4.18.2",
    "sirv": "^2.0.3",
    "vite": "^4.3.6",
    "vue": "^3.3.2"
  }
}
